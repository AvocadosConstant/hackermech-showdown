var body_part = {
  "torso": 250,
  "arm": 100,
  "leg": 100,
}

var starting_power = 100;

function body_part(type) {
  var new_part = {
    "health" 
  }
}

function player(player_id) {
  this.player_id = player_id;
  this.torso = body_part
  this.left_arm = body_part("arm");
  this.right_arm = body_part("arm");
  this.left_leg = body_part("leg");
  this.right_leg = body_part("leg");
}
